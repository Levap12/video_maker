# Video Maker - Автоматизация работы с видео

Полнофункциональный проект для автоматизации работы с видео контентом: от загрузки с HDRezka до создания YouTube Shorts. Специализируется на работе с сериалом "Рик и Морти", но может быть адаптирован для любого контента.

---

## 📋 Содержание

- [Возможности](#-возможности)
- [Структура проекта](#-структура-проекта)
- [Установка](#-установка)
- [Компоненты](#-компоненты)
- [Workflow](#-workflow)
- [Примеры использования](#-примеры-использования)
- [Формат данных](#-формат-данных)
- [Требования](#-требования)
- [Устранение проблем](#-устранение-проблем)

---

## 🎯 Возможности

### Загрузка видео
- ✅ Интерактивная загрузка с HDRezka
- ✅ Массовое скачивание всех сезонов сериала
- ✅ Автоматический выбор озвучки и качества
- ✅ Проверка статуса скачанных файлов

### Обработка видео
- ✅ Нарезка видео на клипы по временным меткам
- ✅ Автоматическое склеивание выбранных клипов
- ✅ Создание компиляций из отрывков

### Создание YouTube Shorts
- ✅ Конвертация в формат 9:16 (вертикальное видео)
- ✅ Добавление размытого фона
- ✅ Поддержка баннеров и водяных знаков
- ✅ Настройка размеров и позиций элементов

### Транскрибация
- ✅ Интеграция с Whisper через Google Colab
- ✅ Форматирование транскрипций
- ✅ Создание временных меток для клипов

---

## 📁 Структура проекта

```
Video_macker3/
├── scripts/                # Основные Python скрипты
│   ├── hdrezka_downloader.py      # Загрузчик с HDRezka
│   ├── mass_downloader.py          # Массовый загрузчик
│   ├── video_clipper.py           # Нарезка клипов
│   ├── video_processor.py          # Обработка и склеивание
│   ├── shorts_creator.py           # Создание YouTube Shorts
│   ├── sample_transcription_demo.py # Демо транскрибации
│   └── audio_extractor.py          # Извлечение аудио
│
├── data/                   # JSON файлы и данные
│   ├── sample_clips.json           # Пример JSON для клипов
│   ├── rick_morty_compilation.json # Пример компиляции
│   ├── test_transcription.txt      # Тестовая транскрипция
│   └── sample_output/              # Примеры выходных файлов
│
├── docs/                   # Документация
│   ├── STRUCTURE.md               # Описание структуры проекта
│   ├── GEMINI.md                  # Документация для AI
│   ├── MASS_README.md             # Инструкция по массовому скачиванию
│   └── SERVER_INSTRUCTIONS.md     # Инструкции для сервера
│
├── utils/                  # Утилиты
│   └── transcription_formatter.py  # Форматирование транскрипций
│
├── google_colab/           # Интеграция с Google Colab
│   └── Транскрибация из аудуи вишпер.ipynb
│
├── server_package/         # Пакет для серверного развертывания
│
├── downloads/              # Скачанные видео файлы (создается автоматически)
├── clips/                   # Нарезанные клипы (создается автоматически)
├── output/                  # Финальные видео (создается автоматически)
├── audio/                   # Извлеченное аудио (создается автоматически)
├── baner/                   # Баннеры для YouTube Shorts
│
├── venv/                    # Виртуальное окружение Python
├── requirements.txt        # Зависимости Python
├── .gitignore              # Правила игнорирования для Git
└── README.md                # Основная документация
```

**Подробное описание структуры:** см. [docs/STRUCTURE.md](docs/STRUCTURE.md)

---

## 🔧 Установка

### 1. Клонирование и переход в директорию

```bash
cd Video_macker3
```

### 2. Создание виртуального окружения (рекомендуется)

**Windows:**
```bash
python -m venv venv
venv\Scripts\activate
```

**Linux/Mac:**
```bash
python3 -m venv venv
source venv/bin/activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Проверка установки

```bash
python scripts/hdrezka_downloader.py --help
```

---

## 🧩 Компоненты

### 1. HDRezka Downloader (`hdrezka_downloader.py`)

Интерактивный загрузчик видео с HDRezka с поддержкой поиска и выбора параметров.

#### Основные функции:
- Поиск контента по названию
- Выбор озвучки и качества
- Загрузка серий с указанием сезона/серии
- Прогресс бар загрузки

#### Использование:

**Интерактивный режим (рекомендуется):**
```bash
python scripts/hdrezka_downloader.py
```

**Командная строка:**
```bash
# Поиск
python scripts/hdrezka_downloader.py --search "рик и морти"

# Прямое скачивание
python scripts/hdrezka_downloader.py --url "URL" --season 1 --episode 1 --quality 720p --translator 66
```

**Популярные озвучки:**
- **66** - Сыендук
- **35** - колдфильм (Coldfilm)
- **316** - октопус (Octopus/Ultradox)
- **355** - HDrezka Studio (18+)
- **232** - TVShows

---

### 2. Mass Downloader (`mass_downloader.py`)

Массовое скачивание всех сезонов сериала "Рик и Морти" (все 8 сезонов, 81 серия).

#### Использование:

```bash
# Интерактивный режим
python scripts/mass_downloader.py

# Скачать все сезоны
python scripts/mass_downloader.py --all

# Скачать конкретный сезон
python scripts/mass_downloader.py --season 1

# Показать статус скачанных файлов
python scripts/mass_downloader.py --status
```

#### Настройки по умолчанию:
- **Озвучка**: Сыендук (ID: 66)
- **Качество**: 360p
- **Папка сохранения**: `downloads/`

#### Информация о сезонах:

| Сезон | Год | Серий |
|-------|-----|-------|
| 1 | 2013–2014 | 11 |
| 2 | 2015 | 10 |
| 3 | 2017 | 10 |
| 4 | 2019–2020 | 10 |
| 5 | 2021 | 10 |
| 6 | 2022 | 10 |
| 7 | 2023 | 10 |
| 8 | 2025 | 10 |

**Всего: 81 серия**

---

### 3. Video Clipper (`video_clipper.py`)

Нарезка видео на отдельные клипы по временным меткам из JSON файла.

#### Использование:

```bash
# Создать пример JSON файла
python scripts/video_clipper.py --sample

# Нарезать клипы
python scripts/video_clipper.py --json data/clips.json --video downloads/video.mp4

# Указать выходную папку
python scripts/video_clipper.py --json data/clips.json --video downloads/video.mp4 --output my_clips
```

#### Формат JSON для клипов:

```json
{
  "start_time": "00:00:02",
  "end_time": "00:00:45",
  "title": "Название клипа",
  "caption": "Описание клипа",
  "type": "shock_mem"
}
```

**Поддерживаемые типы:**
- `shock_mem` - шокирующие моменты
- `funny_dialogue` - смешные диалоги
- `relatable_mem` - узнаваемые моменты
- `quote_mem` - запоминающиеся цитаты
- `visual_adventure` - визуальные приключения

---

### 4. Video Processor (`video_processor.py`)

Нарезка и автоматическое склеивание клипов в одно видео. Обрабатывает только клипы с флагом `join_sequence: true`.

#### Использование:

```bash
# Создать пример JSON файла
python scripts/video_processor.py --sample

# Обработать JSON и создать компиляцию
python scripts/video_processor.py --json data/compilation.json

# Указать выходной файл
python scripts/video_processor.py --json data/compilation.json --output my_compilation.mp4

# Показать доступные исходные видео
python scripts/video_processor.py --list

# Не удалять временные файлы после обработки
python scripts/video_processor.py --json data/compilation.json --no-cleanup
```

#### Формат JSON для компиляции:

```json
{
  "start_time": "00:00:02",
  "end_time": "00:00:08",
  "title": "Рик будит Морти",
  "caption": "Пошли, Морти! У нас важное дело! 🚀",
  "join_sequence": true
}
```

**Важно:** Только клипы с `join_sequence: true` будут склеены в финальное видео.

---

### 5. Shorts Creator (`shorts_creator.py`)

Конвертация видео в формат 9:16 для YouTube Shorts с добавлением эффектов.

#### Возможности:
- Автоматическая адаптация под формат 9:16
- Размытый фон из самого видео
- Добавление баннера (видео файл) сверху
- Текстовый водяной знак снизу
- Настройка размеров и позиций

#### Использование:

```bash
# Обработать все видео из папки clips
python scripts/shorts_creator.py

# Обработать конкретный файл
python scripts/shorts_creator.py --input clips/video.mp4

# С баннером и водяным знаком
python scripts/shorts_creator.py --banner baner/IMG_2196.MOV --watermark-text "@MyChannel" --watermark-color white

# Настройка параметров
python scripts/shorts_creator.py \
  --height-scale 2.0 \
  --banner baner/IMG_2196.MOV \
  --banner-offset 100 \
  --watermark-text "@MyChannel" \
  --watermark-font-size 72 \
  --watermark-bottom-offset 180 \
  --watermark-color gray
```

#### Параметры:

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `--input`, `-i` | Путь к входному видео | Все из `clips/` |
| `--output`, `-o` | Папка для сохранения | `shorts_output/` |
| `--input-dir`, `-d` | Папка с исходными клипами | `clips/` |
| `--height-scale`, `-s` | Коэффициент увеличения высоты | `2.0` |
| `--banner`, `-b` | Путь к баннеру (видео) | Нет |
| `--banner-offset`, `-bo` | Отступ сверху для баннера (px) | `100` |
| `--watermark-text`, `-wt` | Текст водяного знака | Нет |
| `--watermark-font-size`, `-wfs` | Размер шрифта водяного знака | `72` |
| `--watermark-bottom-offset`, `-wbo` | Отступ снизу для водяного знака (px) | `180` |
| `--watermark-color`, `-wc` | Цвет водяного знака | `gray` |

**Доступные цвета:** `white`, `black`, `red`, `blue`, `green`, `yellow`, `orange`, `purple`, `gray`

**Особенности:**
- Баннер появляется через 5 секунд после начала видео
- Баннер растягивается на всю длительность видео (зацикливается если нужно)
- Разрешение выходного видео: 720x1280 (9:16)
- Размытие фона: радиус 20 пикселей

---

### 6. Transcription Formatter (`utils/transcription_formatter.py`)

Утилита для форматирования транскрипций Whisper: удаляет детализацию слов и оставляет только строки с временными метками.

#### Использование:

```bash
# Интерактивный режим
python utils/transcription_formatter.py

# Форматировать файл
python utils/transcription_formatter.py data/input.txt

# Указать выходной файл
python utils/transcription_formatter.py data/input.txt --output data/formatted.txt

# Превью без сохранения
python utils/transcription_formatter.py data/input.txt --preview

# Статистика файла (в интерактивном режиме)
```

#### Формат транскрипции (до форматирования):

```
[0:00:11.020000 - 0:00:12.800000] У меня тут приличный магазин!
  Слова: У(0:00:11.020000-0:00:11.140000) меня(0:00:11.140000-0:00:11.480000) ...
```

#### После форматирования:

```
[0:00:11.020000 - 0:00:12.800000] У меня тут приличный магазин!
[0:00:15.500000 - 0:00:18.200000] Это действительно хороший магазин.
```

---

### 7. Google Colab Integration (`google_colab/`)

Интеграция с Google Colab для транскрибации аудио через Whisper.

#### Файлы:
- `audio_extractor.py` - извлечение аудио из видео
- `Транскрибация из аудуи вишпер.ipynb` - ноутбук для транскрибации
- `AUDIO_EXTRACTOR_README.md` - инструкция по извлечению аудио
- `whisper_colab_instructions.md` - инструкция по транскрибации

#### Процесс:

1. **Извлечение аудио:**
   ```bash
   python google_colab/audio_extractor.py
   ```

2. **Загрузка в Google Colab:**
   - Загрузите аудио файлы в Google Drive
   - Откройте ноутбук `Транскрибация из аудуи вишпер.ipynb`
   - Следуйте инструкциям в ноутбуке

3. **Скачивание транскрипции:**
   - Скачайте результат из Colab
   - Форматируйте через `transcription_formatter.py`

---

### 8. Server Package (`server_package/`)

Готовый пакет для развертывания массового скачивания на сервере Linux.

#### Структура:
```
server_package/
├── mass_downloader.py    # Основной скрипт
├── requirements.txt      # Зависимости
├── setup.sh            # Автоматическая установка
├── run.sh              # Запуск скачивания
├── status.sh           # Проверка статуса
├── stop.sh             # Остановка процесса
└── README.md           # Подробная инструкция
```

#### Развертывание на сервере:

```bash
# 1. Загрузите на сервер
scp -r server_package user@server-ip:/home/user/

# 2. Подключитесь
ssh user@server-ip
cd server_package

# 3. Установите
chmod +x *.sh
./setup.sh

# 4. Запустите
./run.sh
```

#### Управление:

```bash
./run.sh      # Запуск скачивания
./status.sh   # Проверка статуса
./stop.sh     # Остановка процесса
tail -f logs/download.log  # Просмотр логов
```

---

## 🔄 Workflow (Типичный процесс работы)

### 1. Загрузка видео

**Вариант A: Интерактивная загрузка**
```bash
python scripts/hdrezka_downloader.py
```

**Вариант B: Массовое скачивание**
```bash
python scripts/mass_downloader.py --all
```

### 2. Извлечение аудио и транскрибация

```bash
# Извлечение аудио
python google_colab/audio_extractor.py

# Транскрибация в Google Colab
# (используйте ноутбук Транскрибация из аудуи вишпер.ipynb)

# Форматирование транскрипции
python utils/transcription_formatter.py transcription.txt
```

### 3. Создание JSON с клипами

На основе отформатированной транскрипции создайте JSON файл:

```bash
# Создать пример
python scripts/video_clipper.py --sample
```

Затем отредактируйте `sample_clips.json` вручную, выбрав нужные моменты.

### 4. Нарезка клипов

```bash
# Вариант A: Отдельные клипы
python scripts/video_clipper.py --json clips.json --video downloads/video.mp4

# Вариант B: Компиляция (склеивание)
python scripts/video_processor.py --json compilation.json
```

### 5. Создание YouTube Shorts

```bash
python scripts/shorts_creator.py \
  --banner baner/IMG_2196.MOV \
  --watermark-text "@MyChannel" \
  --watermark-color white \
  --height-scale 2.0
```

---

## 📝 Примеры использования

### Пример 1: Скачать один сезон и создать компиляцию

```bash
# 1. Скачать сезон 1
python scripts/mass_downloader.py --season 1

# 2. Создать JSON файл с клипами (вручную или используя пример)
python scripts/video_processor.py --sample
# Отредактируйте sample_clips.json

# 3. Создать компиляцию
python scripts/video_processor.py --json rick_morty_compilation.json

# 4. Создать Shorts версию
python shorts_creator.py --input output/compilation_*.mp4
```

### Пример 2: Массовое создание Shorts из всех клипов

```bash
# 1. Нарезать все клипы
python scripts/video_clipper.py --json clips.json --video video.mp4

# 2. Создать Shorts из всех клипов с баннером
python scripts/shorts_creator.py \
  --input-dir clips \
  --banner baner/IMG_2196.MOV \
  --watermark-text "@MyChannel"
```

### Пример 3: Серверное развертывание

```bash
# На локальной машине: упаковать
cd server_package
zip -r ../server_package.zip .

# На сервере: развернуть
scp server_package.zip user@server:/home/user/
ssh user@server
unzip server_package.zip
cd server_package
chmod +x *.sh
./setup.sh
./run.sh
```

---

## 📄 Формат данных

### JSON для клипов (video_clipper.py)

```json
{
  "start_time": "00:00:02",
  "end_time": "00:00:45",
  "title": "Название клипа",
  "caption": "Описание",
  "type": "shock_mem"
}
```

### JSON для компиляции (video_processor.py)

```json
{
  "start_time": "00:00:02",
  "end_time": "00:00:08",
  "title": "Рик будит Морти",
  "caption": "Пошли, Морти! У нас важное дело! 🚀",
  "join_sequence": true
}
```

**Важно:**
- Время в формате `HH:MM:SS` или `MM:SS`
- Для компиляции нужен флаг `join_sequence: true`
- Название клипа используется в имени файла (недопустимые символы заменяются)

---

## ⚙️ Требования

### Системные требования

**Минимальные:**
- Python 3.7+
- 2GB RAM
- 10GB свободного места
- Интернет соединение

**Рекомендуемые:**
- Python 3.9+
- 4GB+ RAM
- 50GB+ свободного места
- Стабильное интернет соединение

### Зависимости Python

Основные библиотеки (см. `requirements.txt`):
- `HdRezkaApi>=1.0.0` - работа с HDRezka
- `requests>=2.25.0` - HTTP запросы
- `moviepy>=1.0.3` - обработка видео
- `Pillow` - работа с изображениями
- `numpy` - математические операции

### Дополнительные требования

Для обработки видео через MoviePy может потребоваться:
- **FFmpeg** (для Windows: скачать с https://ffmpeg.org/)
- **ImageMagick** (опционально, для некоторых эффектов)

**Установка FFmpeg на Windows:**
1. Скачать с https://ffmpeg.org/download.html
2. Распаковать и добавить в PATH
3. Проверить: `ffmpeg -version`

---

## 🐛 Устранение проблем

### Проблемы с загрузкой

**Скрипт зависает при получении потока:**
```
Решение:
1. Нажмите Ctrl+C для прерывания
2. Проверьте интернет-соединение
3. Попробуйте другую озвучку
4. Попробуйте позже
```

**Ошибка "Не удалось получить поток":**
```
Решение:
- Проверьте, что сезон и серия существуют
- Попробуйте другую озвучку
- Убедитесь, что сайт HDRezka доступен
```

### Проблемы с обработкой видео

**Ошибка "FFmpeg not found":**
```
Решение (Windows):
1. Скачать FFmpeg с https://ffmpeg.org/
2. Распаковать в C:\ffmpeg
3. Добавить C:\ffmpeg\bin в PATH
4. Перезапустить терминал
```

**Ошибка памяти при обработке:**
```
Решение:
- Используйте видео меньшего разрешения
- Обрабатывайте файлы по одному
- Закрывайте другие приложения
```

**Видео без звука:**
```
Решение:
- Проверьте исходное видео
- Убедитесь, что audio_codec='aac' в коде
```

### Проблемы с Shorts Creator

**Баннер не появляется:**
```
Решение:
- Проверьте путь к баннеру
- Убедитесь, что баннер - видео файл (не изображение)
- Проверьте формат видео баннера
```

**Водяной знак не виден:**
```
Решение:
- Попробуйте другой цвет (белый или черный)
- Увеличьте размер шрифта (--watermark-font-size)
- Проверьте позицию (--watermark-bottom-offset)
```

**Видео выглядит растянутым:**
```
Решение:
- Настройте --height-scale (попробуйте 1.5 или 1.8)
- Проверьте исходное соотношение сторон видео
```

### Проблемы с транскрибацией

**Транскрипция пустая:**
```
Решение:
- Проверьте, что аудио файл корректен
- Убедитесь, что Whisper модель запущена
- Проверьте язык распознавания в ноутбуке
```

**Транскрипция с ошибками:**
```
Решение:
- Используйте более крупную модель Whisper (medium или large)
- Проверьте качество аудио
- Убедитесь, что язык указан правильно
```

### Общие проблемы

**Импорт ошибок (ModuleNotFoundError):**
```bash
# Решение:
pip install -r requirements.txt
```

**Ошибки с кодировкой (Windows):**
```bash
# Решение: установите кодировку UTF-8
chcp 65001
```

**Медленная обработка:**
```
Решение:
- Используйте видео меньшего разрешения
- Закройте другие приложения
- Обрабатывайте файлы последовательно
- Используйте SSD вместо HDD
```

---

## 📞 Дополнительная информация

### Полезные команды

```bash
# Проверить статус скачанных файлов
python scripts/mass_downloader.py --status

# Показать доступные исходные видео
python scripts/video_processor.py --list

# Создать пример JSON файла
python scripts/video_clipper.py --sample
python scripts/video_processor.py --sample
```

### Советы по оптимизации

1. **Для массового скачивания:**
   - Используйте серверный пакет
   - Запускайте на ночь
   - Используйте качество 360p для экономии места

2. **Для обработки видео:**
   - Обрабатывайте по одному файлу
   - Используйте SSD для временных файлов
   - Закрывайте другие приложения

3. **Для создания Shorts:**
   - Используйте предустановленные баннеры
   - Тестируйте на одном файле перед массовой обработкой
   - Настройте параметры под ваш стиль

### Логи и отладка

Большинство скриптов выводят подробную информацию в консоль. Для сохранения логов:

```bash
# Windows PowerShell
python script.py *> log.txt

# Linux/Mac
python script.py 2>&1 | tee log.txt
```

---

## 📜 Лицензия

Этот проект создан для личного использования. Убедитесь, что использование контента с HDRezka соответствует их условиям использования.

---

## 🎉 Готово!

Теперь вы готовы использовать все возможности проекта. Если возникнут вопросы, обращайтесь к этой документации или к примерам использования выше.

**Удачи в создании контента! 🚀**
