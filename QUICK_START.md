# Быстрый старт для деплоя

## Шаг 1: Подготовка к загрузке на GitHub

1. **Создайте файл `.env`** (он уже в .gitignore, не будет загружен):
   - Скопируйте содержимое из `env.example`
   - Заполните реальными значениями API ключей

2. **Проверьте `.gitignore`** - убедитесь, что чувствительные данные не попадут в репозиторий:
   - `.env` файлы
   - `profiles/` директория
   - `data/task_state.json`
   - Логи и медиа файлы

3. **Инициализируйте Git репозиторий** (если еще не сделано):
```bash
git init
git add .
git commit -m "Initial commit"
```

4. **Создайте репозиторий на GitHub** и загрузите код:
```bash
git remote add origin https://github.com/yourusername/Video_macker3.git
git branch -M main
git push -u origin main
```

## Шаг 2: Деплой на сервер

### Вариант A: Docker (Самый простой)

На сервере выполните:

```bash
# Клонируйте репозиторий
git clone https://github.com/yourusername/Video_macker3.git
cd Video_macker3

# Создайте .env файл
cp env.example .env
nano .env  # Заполните переменные

# Запустите
# Docker Compose V2 (рекомендуется)
docker compose up -d --build
# Или Docker Compose V1
# docker-compose up -d --build
```

### Вариант B: Без Docker

```bash
# Клонируйте репозиторий
git clone https://github.com/yourusername/Video_macker3.git
cd Video_macker3

# Создайте виртуальное окружение
python3 -m venv venv
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt

# Создайте .env файл
cp env.example .env
nano .env  # Заполните переменные

# Запустите
export FLASK_ENV=production
python run_web.py
```

## Шаг 3: Проверка

Откройте в браузере: `http://your-server-ip:5000`

## Обновление

```bash
git pull
# Docker Compose V2 (рекомендуется)
docker compose up -d --build  # для Docker
# Или Docker Compose V1
# docker-compose up -d --build
# или
sudo systemctl restart video-maker  # для systemd
```

## Важные замечания

⚠️ **Безопасность:**
- Никогда не коммитьте `.env` файл
- Измените `SECRET_KEY` на случайную строку
- Для продакшена используйте nginx с SSL

📝 **Подробные инструкции:** См. `DEPLOY.md`

